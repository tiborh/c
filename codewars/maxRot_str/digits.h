#ifndef DIGITS_H
#define DIGITS_H

#include <math.h>
#include <stdlib.h>
#include <string.h>

int num_of_digits(long long);
char* unshift_digits(char *,char *);
char* first_to_last(char *);
char* push_digit(char *,char *);
char* first_digit(char *);
char* rest_digits(char *);

#endif // DIGITS_H 
