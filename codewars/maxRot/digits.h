#ifndef DIGITS_H
#define DIGITS_H

#include <math.h>

long long unshift_digits(long long,long long);
long long first_to_last(long long);
long long push_digit(long long, int);
int first_digit(long long);
long long rest_digits(long long);

#endif // DIGITS_H 
