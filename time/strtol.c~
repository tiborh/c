#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {

  long int intput;

  if (argc < 2)
    printf("Usage: you at least one argument string");
  else {
    intput = strtol(argv[1],NULL,10);
    printf("The number parsed: %ld",intput);
  }

  return 0;
}
