/*	Generic function to determine the larger of two	values referenced as void pointers.	   Pre  dataPtr1 and dataPtr2 are pointers to values	           of an unknown type.	        ptrToCmpFun is address of a function that	           knows the data types	   Post data compared and larger value returned*/void* larger (void* dataPtr1,    void* dataPtr2,               int (*ptrToCmpFun)(void*, void*)){	if ((*ptrToCmpFun) (dataPtr1, dataPtr2) > 0)	      return dataPtr1;	else	      return dataPtr2;}	// larger 